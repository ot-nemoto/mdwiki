<%= render(:partial => 'menu', :locals => { :url => @url, :id => @page.id }) %>
<%= render(:partial => 'breadcrumb', :locals => { :breadcrumb => @breadcrumb, :title => @title }) %>

<div class="container"><%= @page.md_to_html.html_safe %></div>
