<%= link_to('TOP', {controller => 'mdwiki', :action => 'main'}) %>
<% @breadcrumb_list.each do |breadcrumb| %>
  <%= content_tag(:span, '/') %> 
  <%= link_to(breadcrumb[:title], \
              {controller => 'mdwiki', :action => 'show', :id => breadcrumb[:id]}) %>
<% end %>
