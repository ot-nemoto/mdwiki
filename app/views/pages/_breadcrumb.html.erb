<%= link_to 'TOP', {controller => 'mdwiki', :action => 'main'} %>
<% breadcrumb_list.reverse.each do |breadcrumb| %>
  <%= content_tag :span, '/' %> 
  <%= link_to breadcrumb.subject.to_s, "/mdwiki/#{breadcrumb.content_id.to_s}" %>
<% end %>
